"use client";
import{useEffect,useState}from"react";
import{useRouter}from"next/navigation";
type Warehouse={inventoryId:string;warehouseId:string;warehouseName:string;location:string;totalStock:number;reserved:number;available:number};
type Product={id:string;name:string;description:string;price:number;warehouses:Warehouse[]};
export default function Home(){const router=useRouter();const[products,setProducts]=useState<Product[]>([]);const[loading,setLoading]=useState(true);const[reserving,setReserving]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);useEffect(()=>{fetch("/api/products").then(r=>r.json()).then(d=>{setProducts(d);setLoading(false)});},[]);async function handleReserve(inventoryId: string,warehouseName: string){setReserving(inventoryId);setError(null);try{const res=await fetch("/api/reservations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({inventoryId,quantity:1})});const data=await res.json();if(res.status===409){setError("Not enough stock at "+warehouseName);setReserving(null);return;}if(!res.ok){setError(data.error||"Error");setReserving(null);return;}router.push("/reservation/"+data.id);}catch{setError("Network error");setReserving(null);}}if(loading)return(<div className="min-h-screen flex items-center justify-center"><p className="text-gray-500">Loading...</p></div>);return(<main className="min-h-screen bg-gray-50 py-10 px-4"><div className="max-w-5xl mx-auto"><h1 className="text-3xl font-bold text-gray-900 mb-2">Allo Store</h1></div></main>);}